apiVersion: v1
kind: Service
metadata:
    name: "couponhub-b676"
    labels:
        app: "couponhub-b676"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "couponhub-b676"